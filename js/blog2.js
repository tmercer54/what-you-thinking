const blogHistory = JSON.parse(localStorage.getItem("blogHistory")) || [];
for(let i=0; i<blogHistory.length; i++){
    //loop through blog history array and add array object content and add to blog2 HTML
    
}

